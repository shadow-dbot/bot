const Discord = require("discord.js");
const Moment = require("moment");
const path = require("path");

const db = require("../models");
const checkDB = require("../utils/checkDB");
const Config = require("../config/key");

module.exports = {
	test: async (client, guild) => {},
};
